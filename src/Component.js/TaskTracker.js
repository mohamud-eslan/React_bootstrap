import React, {useState} from 'react'

const TaskTracker = () => {
  const name = 'ahmed'
  return (
    <div>
        <h1>hi {4 + 3}</h1>
        {name ? (
          <>
          test
          </>
        ):
        (
          test
        )}
    </div>
  )
}

export default TaskTracker